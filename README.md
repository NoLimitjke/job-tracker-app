# Job Application Tracker

Веб-приложение для отслеживания откликов на вакансии (Job Application Tracker), разработанное на **Next.js 16**.  
Приложение позволяет удобно управлять процессом поиска работы, перемещая отклики между стадиями с помощью drag-and-drop.

**🌐Демо**

Приложение задеплоено на Vercel и доступно по ссылке:
👉 https://job-tracker-app-taupe.vercel.app/

## 🚀 Возможности

- Добавление и редактирование откликов на вакансии
- Разделение откликов по статусам (например: Applied, Interview, Offer, Rejected)
- Перемещение откликов между колонками с помощью **drag-and-drop**
- Хранение данных в **MongoDB**
- Современный стек на базе App Router (Next.js 16)

## 🛠 Технологии

- **Next.js 16**
- **React**
- **MongoDB** (через Mongoose)
- **dnd-kit** — drag-and-drop между колонками
- **TypeScript** 
- **CSS / Tailwind CSS** 


## 🧩 Drag and Drop

Для реализации перемещения откликов между статусами используется библиотека **dnd-kit**.  
Она позволяет:

- Перетаскивать карточки между колонками
- Обновлять статус отклика без перезагрузки страницы
- Гибко управлять логикой сортировки и позиционирования

## 🗄 База данных

В качестве базы данных используется **MongoDB**.

Пример сущности `JobApplication`:

```ts
{
  title: string,
  company: string,
  status: string,
  createdAt: Date,
  updatedAt: Date
}
```

⚙️ Установка и запуск

Клонировать репозиторий:
```bash

git clone https://github.com/username/job-application-tracker.git
```

Установить зависимости:
```bash
npm install
```

Создать файл .env.local и указать строку подключения к MongoDB:
```env
MONGODB_URI=your_mongodb_connection_string
```

Запустить проект:
```bash
npm run dev
```
Открыть в браузере:
```bash
http://localhost:3000
```

📌 Цель проекта

Проект создан для практики работы с:

Next.js (App Router)

MongoDB

drag-and-drop интерфейсами

построением CRUD-приложений
